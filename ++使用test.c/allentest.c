#include<stdio.h>
int main()
{
    
    
    int i;
        if (i > sizeof(i))
        {
            printf(">\n");
        }
        else
        {
            printf("<\n");
        }
        return 0;
	//int i = 0;
	//int ret = (++i) + (++i) + (++i);
	//printf("%d", ret);
	return 0;
}